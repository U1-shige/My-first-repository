# iOSショートカット設定手順

## 事前準備

### Boxをファイルアプリに追加する

1. iPhoneで **ファイル** アプリを開く
2. 右下の「**ブラウズ**」をタップ
3. 右上の「**…**」をタップ →「**編集**」
4. 「Box」の横のスイッチを **オン** にする
5. 右上「**完了**」

### Boxにフォルダを作成する（box.comまたはBoxアプリから）

```
Claude/
  ├── prompts/    ← ここにprompt.txtを置く
  └── results/    ← ここに結果が保存される
```

---

## ショートカット1：「Claudeに送る」

> **やること**: BoxからプロンプトをコピーしてClaude.aiを開く

### 開き方
1. **ショートカット** アプリを開く
2. 右上の「**＋**」をタップ
3. 画面下の「**アクションを追加**」をタップ

---

### アクション1：ファイルを取得

1. 検索欄に「**ファイルを取得**」と入力
2. 「ファイルを取得」をタップ
3. アクションが追加される。青文字の「**ファイル**」をタップ
4. ファイル選択画面が開く → 「**Box**」をタップ
5. Claude → prompts → **prompt.txt** をタップして選択
6. アクションの右側の「**∨**」（下矢印）をタップして展開
7. 「**ファイルピッカーを表示**」のスイッチを **オフ** にする

---

### アクション2：クリップボードにコピー

1. 画面下の「**＋**」をタップ → 検索欄に「**クリップボード**」
2. 「**クリップボードにコピー**」をタップ
3. 「コピー」の後ろの部分をタップ
4. 「**ショートカット入力**」が選択肢に出る → タップ
   （前のステップのファイル内容が自動でセットされます）

---

### アクション3：URLを開く

1. 「**＋**」→ 検索欄に「**URLを開く**」
2. 「URLを開く」をタップ
3. URL欄に `https://claude.ai/new` と入力

---

### 保存
- 右上「**完了**」→ ショートカット名を「**Claudeに送る**」に変更 → 完了

---

## ショートカット2：「Boxに保存」

> **やること**: コピーしたClaude返答を日時付きファイル名でBoxに保存する

### 開き方
ショートカットアプリ → 右上「＋」→「アクションを追加」

---

### アクション1：現在の日付を取得

1. 検索欄に「**現在の日付**」と入力
2. 「現在の日付」をタップ

---

### アクション2：日付をフォーマット

1. 「**＋**」→ 検索欄に「**日付をフォーマット**」
2. 「日付をフォーマット」をタップ
3. 「日付」の部分をタップ →「**現在の日付**」（マジック変数）を選択
4. 「形式」→「**カスタム**」を選択
5. カスタム形式の欄に `yyyyMMdd_HHmmss` と入力
   （例：20260520_143022 という文字列になります）

---

### アクション3：テキスト（ファイル名を作る）

1. 「**＋**」→ 検索欄に「**テキスト**」
2. 「テキスト」をタップ
3. テキスト欄に `result_` と入力
4. そのまま続けて、キーボード上の「**変数を挿入**」（または入力欄を長押し）→「**日付をフォーマット**」をタップ
5. 続けて `.txt` と入力
6. 完成形：`result_[日付をフォーマット].txt`

---

### アクション4：ファイルを保存

1. 「**＋**」→ 検索欄に「**ファイルを保存**」
2. 「ファイルを保存」をタップ
3. 「入力」の部分をタップ →「**クリップボード**」を選択
4. 「保存先」→「**Box**」→ Claude → results（フォルダを選択）
5. アクションの「**∨**」をタップして展開
6. 「**名前**」欄をタップ → 上で作った「**テキスト**」変数を選択
7. 「**確認する**」スイッチを **オフ** にする（毎回確認ダイアログが出なくなる）
8. 「**上書き**」スイッチを **オン**（同名ファイルがあれば置き換え）

---

### 保存
右上「**完了**」→ ショートカット名を「**Boxに保存**」に変更 → 完了

---

## ホーム画面に追加する（便利）

1. ショートカットアプリ → 「Claudeに送る」を長押し →「詳細」
2. 「**ホーム画面に追加**」→「追加」
3. 「Boxに保存」も同様に追加
4. ホーム画面に2つのボタンが並ぶ

---

## 毎日の使い方まとめ

```
【PC】
prompt.txt を編集 → box.com の Claude/prompts/ にアップロード

【iPhone】
① 「Claudeに送る」をタップ
   → Boxから自動取得 → クリップボードへ → Claude.aiが開く

② 貼り付け（テキスト欄を長押し→「ペースト」）
   → 送信ボタンをタップ
   → 回答が表示されたらコピー（回答を長押し→「コピー」）

③ 「Boxに保存」をタップ
   → result_20260520_143022.txt として自動保存

【PC】
box.com → Claude/results/ で結果ファイルを確認
```
